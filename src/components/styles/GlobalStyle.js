import { createGlobalStyle } from "styled-components";

export const GlobalStyle = createGlobalStyle`
  * {
    text-decoration: none;
  }
  body {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    background: rgba(224,228,226,1);
  }
`;
